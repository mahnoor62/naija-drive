import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Naija Drive - Vehicle Marketplace",
  description: "Purchase premium vehicles for your Naija Drive racing game. Get instant redeem codes to unlock cars in-game!",
  keywords: "Naija Drive, racing game, vehicles, cars, marketplace, gaming, Nigeria",
  authors: [{ name: "Naija Drive Team" }],
  icons: {
    icon: "data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🚗</text></svg>",
  },
  openGraph: {
    title: "Naija Drive - Vehicle Marketplace",
    description: "Purchase premium vehicles for your Naija Drive racing game. Get instant redeem codes to unlock cars in-game!",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
